apiVersion: v1
kind: Service
metadata:
  name: {{ include "medical-forum.fullname" . }}
  labels:
    {{- include "medical-forum.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "medical-forum.selectorLabels" . | nindent 4 }}
